# 报表处理工具客户使用说明

## 直接解压使用

把客户交付 ZIP 完整解压到一个普通文件夹，再双击根目录中的
`报表处理工具.exe`。不要在压缩包预览窗口内直接运行 EXE。

程序是 Windows 免安装版本，不需要安装 Python，也不需要把配置文件
移动到其他目录。

## 打开程序

双击 `报表处理工具.exe`。程序会打开本机浏览器页面，首页提供两个独立入口：

- `报关单生成`：上传 Invoice 和 Packing list，预览后生成报关单。
- `报表合并`：上传 1～30 份源报表，预览后生成合并结果。

程序只在当前 Windows 电脑本机运行，不会把文件上传到外部服务器。

## 配置维护

两个功能各自有“规则与模板”页签，共有四类独立配置：

1. 报关单生成模板。
2. 报关单匹配规则。
3. 报表合并生成模板。
4. 报表合并规则。

客户包中的配置文件是维护参考和初始资料。修改后，需要在对应功能的“规则与模板”页签重新上传并启用。程序不会自动读取 EXE 旁边被替换的文件。

上传新配置时，程序会先验证文件；验证通过才启用，并保留本机版本副本。四类文件分别保存，互不覆盖。

## 历史记录

两个功能分别保存自己的历史记录：

- 最近记录只显示最近 5 条，可逐条删除。
- 历史查询支持按开始时间和结束时间筛选。
- 每页最多显示 50 条，可翻页。
- 报关单历史可下载当时的 Invoice、Packing、预览和报关单。
- 合并历史可下载源报表、当时使用的模板和规则、预览和合并结果。

删除历史记录时，会同时删除该记录保存在本机的文件。

## Windows 数据位置

报关单生成数据：

```text
%LOCALAPPDATA%\SuriWorkDeclaration\
```

报表合并数据：

```text
%LOCALAPPDATA%\SuriWorkReportMerge\
```

两套数据目录相互独立。误删其中一套配置不会影响另一个功能，但对应功能会恢复使用 EXE 内置的默认配置。

## 客户包内容

- `报表处理工具.exe`
- `README_客户使用说明.md`
- `报关单配置资料\报关单生成模板.xlsx`
- `报关单配置资料\报关单模板-标记版（维护参考）.xlsx`
- `报关单配置资料\模板标记维护说明.md`
- `报关单配置资料\报关单配置关系表.xlsx`
- `报关单配置资料\报关单生成取值逻辑说明.docx`
- `报关单配置资料\报关单配置关系表说明文档.docx`
- `报关单配置资料\README_最新逻辑.md`
- `报表合并配置资料\报表合并生成模板.xlsx`
- `报表合并配置资料\报表合并规则.xlsx`
- `报表合并配置资料\合并逻辑说明.md`

业务规则以客户包内最新说明文档为准。
