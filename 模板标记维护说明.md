# 报关单模板标记维护说明

## 核心规则

以后建议使用 `报关单模板-标记版.xlsx` 作为报关单模板。

模板里所有 `{{...}}` 都是程序识别位置的标记。生成报关单时，程序会把这些标记替换成实际数据。

日期标记使用 `{{申报日期}}`，应放在“申报日期”下方；“出口日期”由程序保持空白。

## 可以修改

- 可以调整字体、颜色、边框、行高、列宽。
- 可以移动字段位置，但必须把对应的 `{{...}}` 标记一起移动。
- 可以增加商品可填写行数，做法是复制一整组商品行标记。

## 不要修改

- 不要删除 `{{...}}` 标记。
- 不要改标记文字，例如不要把 `{{合同协议号}}` 改成 `{{合同号}}`。
- 不要只复制商品样式而漏复制商品标记。

## 商品行标记

每一个商品分组需要保留一组标记：

- `{{商品项号}}`
- `{{商品编号}}`
- `{{商品名称}}`
- `{{数量}}`
- `{{数量单位}}`
- `{{金额}}`
- `{{币制}}`
- `{{原产国}}`
- `{{最终目的国}}`
- `{{境内货源地}}`
- `{{征免}}`
- `{{品牌}}`
- `{{净重}}`
- `{{净重单位}}`

当前标记版模板预留了 10 个商品分组。超过 10 个商品分组时，程序会提示模板商品明细标记不足，避免生成错位文件。

## 合计行标记

合计区需要保留：

- `{{合计标签}}`
- `{{合计数量}}`
- `{{合计数量单位}}`
- `{{合计金额}}`
- `{{合计币制}}`
- `{{合计净重}}`
- `{{合计净重单位}}`

## 建议流程

每次更新模板后，先用一组样例生成测试稿，确认商品区、合计区、声明区没有错位，再交付客户使用。
