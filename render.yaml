services:
  - type: web
    name: imos-declaration
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: PUBLIC_TOKEN
        sync: false
      - key: ADMIN_TOKEN
        sync: false
